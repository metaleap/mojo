
userName := /env("USER")


main :=
	i := 0
	\ i < 10
		myProg(userName)
		i = i+1
	<- 0


myProg := (user)
	/defer
		/print user, "'!\n"

	? /strEq(user, "_")
		user = "kingOfCastle"
	| /strEq("root")
		user = "wannabe"
	| /strEq("root")
		user = "never"
	|
		/print "DBG"

	/print (/env("GOPATH").len == 0) ? "Hello, '" | "Hello, Gopher '"
